name = "cghyyrrt/moonbit-pcap"

version = "0.1.0"

readme = "README.md"

repository = "https://github.com/cghyyrrt/moonbit-pcap"

license = "Apache-2.0"

keywords = [
  "pcap",
  "pcapng",
  "network",
  "packet",
  "protocol",
  "reassembly",
  "tcp",
  "dns",
]

description = "Pure MoonBit network packet capture (PCAP / PCAPNG) parser, stream reassembly, and multi-protocol analyzer library"
